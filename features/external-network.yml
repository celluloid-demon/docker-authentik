---
services:
  ak-postgresql:
    networks:
    - nginx-proxy-manager

  ak-server:
    networks:
    - nginx-proxy-manager

  ak-worker:
    networks:
    - nginx-proxy-manager

networks:
  nginx-proxy-manager:
    external: true
